[project]
name = "hello-there"
version = "1.0.0"
description = "Portfolio page that leverages Python, Docker, Tailwind with help of GitHub Actions & SonarQube."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-fasthtml==0.12.41",
    "uvicorn==0.40.0",
]
